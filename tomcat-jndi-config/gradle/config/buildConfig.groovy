environments {
    dev {
        app {
            compilePropertyFile = 'compile-dev.properties'
        }
    }

    interserv {
        app {
            compilePropertyFile = 'compile-prod.properties'
        }
    }
}